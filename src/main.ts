import './style.css'

console.log('SvitUA Frontend Clone loaded!')

// Add your TypeScript code here
document.addEventListener('DOMContentLoaded', () => {
    const app = document.getElementById('app')
    if (app) {
        app.innerHTML = '<h1 class="text-3xl font-bold text-blue-600">SvitUA Clone</h1>'
    }
})
